training:
  epochs: 50
  batch_size: 16
  learning_rate: 0.0001
  weight_decay: 0.0005
  optimizer: "AdamW"
  scheduler: "CosineAnnealingLR"
  save_interval: 5
  checkpoint_dir: "checkpoints"
  device: "cuda"
